use ::minecraft::util::Color;

compound Cat extends super::Tamable {
	/// The type of the cat
	variant: id(minecraft:cat_variant),
	/// The cat's collar color, present for stray cats
	CollarColor: Color
}

Cat describes minecraft:entity[minecraft:cat];
